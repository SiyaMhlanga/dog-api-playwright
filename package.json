{
  "name": "dog-api-playwright",
  "version": "1.0.0",
  "description": "Playwright API test: the Dog CEO API lists \"english\" as a sub-breed of \"mastiff\"",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "report:monocart": "monocart show-report monocart-report/index.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "monocart-reporter": "^2.13.1"
  }
}
